summaries:
  Filled: values.filter(!value.isType("null")).length
properties:
  note.summary:
    displayName: define
views:
  - type: table
    name: wiki
    filters:
      and:
        - file.inFolder("wiki")
    order:
      - file.name
      - define
    columnSize:
      file.name: 185
