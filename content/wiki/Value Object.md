---
defines:
  - Value Object(값 객체): 별도의 식별자가 아니라 값 자체로 구분되며, 값의 의미와 규칙을 하나의 타입으로 표현하는 객체이다.
tags:
  - value_object
  - valueobject
  - 값_객체
  - VO
---
## Value Object(값 객체)

별도의 식별자가 아니라 값 자체로 구분되며, 값의 의미와 규칙을 하나의 타입으로 표현하는 객체이다.

금액, 수량, 이메일, 주소처럼 값의 의미나 제약을 명확하게 표현할 때 사용할 수 있다.
