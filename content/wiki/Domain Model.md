---
defines:
  - Domain Model(도메인 모델): 애플리케이션이 해결하려는 문제 영역의 핵심 개념과 규칙을 표현하는 모델이다.
tags:
  - domain_model
  - 도메인_모델
---
## Domain Model(도메인 모델)

애플리케이션이 해결하려는 문제 영역의 핵심 개념과 규칙을 표현하는 모델이다.

서버 응답이나 DB 테이블의 형태보다 애플리케이션에서 사용하는 상품, 주문, 가격, 재고와 같은 개념을 중심으로 표현한다.
